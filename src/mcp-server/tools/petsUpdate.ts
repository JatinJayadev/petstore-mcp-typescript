/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { petsUpdate } from "../../funcs/petsUpdate.js";
import { Pet$zodSchema } from "../../models/pet.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: Pet$zodSchema.describe(`Update an existent pet in the store`),
};

export const tool$petsUpdate: ToolDefinition<typeof args> = {
  name: "pets-update",
  description: `Update an existing pet

Update an existing pet by Id`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await petsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
